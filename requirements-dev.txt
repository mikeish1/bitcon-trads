# Dev/test-only dependencies (not needed to run the bots).
#   pip install -r requirements-dev.txt
#   pytest -q
pytest>=8.0

# Optional: only needed to serve the capital-settings REST API
# (src/api/capital_settings_api.py). The bots never import these.
# fastapi>=0.110
# uvicorn>=0.29
