# Core dependencies. Lower bounds (not hard pins) so they install cleanly across
# recent Python versions.
ccxt>=4.4,<5            # Binance.US spot data + order execution
pandas>=2.2             # candle dataframes
numpy>=1.26             # numerical helpers
ta>=0.11                # technical indicators (pure python)
anthropic>=0.69         # Claude API (sparing borderline validation + summaries)
loguru>=0.7             # clean logging (renders nicely in Railway)
PyYAML>=6.0             # reads config/trading_config.yaml
python-dotenv>=1.0      # loads .env locally
tenacity>=9.0           # retry/backoff for flaky network calls
